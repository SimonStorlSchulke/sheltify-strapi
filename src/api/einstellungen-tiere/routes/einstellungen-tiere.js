'use strict';

/**
 * einstellungen-tiere router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::einstellungen-tiere.einstellungen-tiere');
