'use strict';

/**
 * einstellungen-tiere service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::einstellungen-tiere.einstellungen-tiere');
